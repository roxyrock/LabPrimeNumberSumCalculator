# Sum of Primes — README

## Overview

- free CodeCamp Lab

This project implements two JavaScript functions:
- primeNumbers(num) — checks whether a given number is prime.
- sumPrimes(number) — calculates the sum of all prime numbers from 2 up to the provided value.